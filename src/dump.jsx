/*const listItems2 = lowCalFruits.map((lowCalFruit) => (
  <li key={lowCalFruit.id}>
    {lowCalFruit.name}: &nbsp; <b>{lowCalFruit.calories}</b>
    </li>
));
const listItems3 = highCalFruits.map((highCalFruit) => (
  <li key={highCalFruit.id}>
    {highCalFruit.name}: &nbsp; <b>{highCalFruit.calories}</b>
    </li>



<li className="lists">lowCalFruits <ol>{listItems2}</ol></li>
     <li className="lists">HighCalFruits <ol>{listItems3}</ol></li>
  */  

  /*   const lowCalFruits = itemList.filter(item=>item.calories < 100);
  const highCalFruits = itemList.filter(item=>item.calories >= 100);*/